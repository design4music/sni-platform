# Resume — 2026-04-17 end of day

If this session breaks, this is where we were.

## Branch / commits shipped today (on main, pushed)

```
cfb6ba5  feat(frontend): dominant theme chips + narratives sidebar (replaces signals)
3c21123  feat(frontend): centroid page hero calendar + enriched track cards
0b7215e  feat(frontend): theme-colored calendar activity chart
bec07aa  feat(scripts): backfill prose + daily briefs by month (ignores freeze)
44942a4  fix(daemon): drop redundant DISTINCT in describe-promoted CTM query
```

All four frontend commits are live on worldbrief.info (manual frontend deploy was done).
Render worker also redeployed with the SQL fix; pipeline processing fine.

## Running tasks (local)

- Background task `b8zaycme4`: `scripts/backfill_prose_by_month.py --month 2026-03-01`
  - Log: `out/march_backfill/run.log`
  - Status at 16:00 UTC: prose 126/135 CTMs, 88.9% of promoted events have DE title
  - When prose finishes, it auto-runs the briefs pass (generates `daily_briefs` rows)
  - ETA: ~2 hours more

### Resume check
```bash
# Is the bg task still running / how far?
tail -20 out/march_backfill/run.log
grep -c "^\[prose" out/march_backfill/run.log
```

## What's done on March local DB

- Events: 37,754 promoted
- DE titles: ~33,560 / 37,754 (88.9%) — remainder actively being filled
- Daily briefs: still only 24 from a smoke test — the briefs pass hasn't started yet, will auto-run after prose completes
- A handful of `LLM fail: Failed to parse LLM response as JSON` entries in log — negligible count, can be re-run on any event where title_de ends up NULL

## Next steps (in order)

### 1. Finish March (today/overnight)
- Let the background task complete both passes
- Spot-check a few CTMs visually on local dev server: `http://localhost:3000/c/AMERICAS-USA/t/geo_security/calendar?month=2026-03`
- Expect: chart fully colored, briefs present on dense days, track cards have prose summaries

### 2. Push March to Render
- Script exists: `out/beats_reextraction/push_april_to_render.py` (hardcoded MONTH; adapt or copy for March)
- Needs preflight: verify Render schema has `events_v3.is_promoted` + `daily_briefs` table (it does — April used the same path)
- Pattern: per-CTM transaction, DELETE Render rows, INSERT local rows. See the April script for FK order.
- **Before running**: pause Render worker to avoid race conditions, then restart after push.

### 3. Feb reprocess (NEXT MAJOR WORK)
User's instruction: "same as March, but entirely — Phase 3 onwards, re-clustering with new labels, regenerating prose, everything."

**This is NOT what `backfill_prose_by_month.py` does.** That script only runs prose+briefs on already-clustered+promoted data. Feb still has legacy v2 labels and old clustering.

The right pattern is the April reprocess one:
- `out/beats_reextraction/pull_april_from_render.py` — pulls the raw inputs (titles_v3, title_assignments from Phase 2) locally
- `out/beats_reextraction/reprocess_april_local.py` — for each CTM: re-extract labels (Phase 3.1 with v3.0.1 prompt), reassign tracks (Phase 3.3 mechanical), recluster (Phase 4 day-centric + same-day merge), promote (4.5a), generate prose (4.5a-describe)
- `out/beats_reextraction/push_april_to_render.py` — push everything back

For Feb we need equivalents:
- `pull_feb_from_render.py` (copy, change MONTH)
- `reprocess_feb_local.py` (copy, change MONTH, make sure it uses the current pipeline functions)
- `push_feb_to_render.py` (copy, change MONTH)
- Plus daily briefs pass (`scripts/backfill_prose_by_month.py --month 2026-02-01 --only briefs`)

Better long-term: parameterize the scripts with `--month`. Right now they're month-hardcoded.

### 4. Jan reprocess
Same pattern as Feb. Last remaining month.

## Open tickets filed today

1. [Pipeline daemon: persist last_run across restarts](https://app.asana.com/1/1211666582649030/project/1211666445056038/task/1214103726997112) — restart makes all slots fire immediately; low priority
2. [CTM digests: restore + modernize period-level summaries](https://app.asana.com/1/1211666582649030/project/1211666445056038/task/1214109791690038) — ctm.summary_text is stale post-D-058
3. [Narrative matching: investigate why Mar/Apr show 0 matches](https://app.asana.com/1/1211666582649030/project/1211666445056038/task/1214106015283570) — daemon runs Phase 4.2f in Slot 3 but nothing written for 2 months

## Strategic plan snapshot (item by item)

Decided earlier today:
- Beats taxonomy: LOCK
- Families: dead, remove after all months reworked (NOT today)
- Friction nodes: revive, reframe as cross-centroid co-moving themes (future)
- Narratives: keep, now surfaced in centroid sidebar (DONE)
- Signals: trimmed from centroid sidebar (DONE)
- Epics: will kill, migrate curated timelines into narratives (later)
- Centroid daily view: DONE (hero + day popover shipped)
- CTM tagging (theme chips): DONE

## Known UI nuances (worth remembering)

- **Theme chips** show top-3 sector/subject pairs per CTM. Currently on: (a) CTM calendar hero header, (b) every enriched TrackCard on centroid page.
- **Day popover** on centroid hero: click a day bar → popover with per-track breakdown + source counts + deep-links. Covers legend/axis area intentionally. Outside-click closes.
- **Active Narratives sidebar** on centroid page: top 5 by distinct event count for this centroid+month. Foreign-framed narratives get an amber "from {Actor}" chip.
- **Color palette** for stacked charts: Tailwind 400/500/600/700 tints of the track base color (red/sky/amber/emerald). Top sector of the month = brightest tint.
- **Legacy fallback** on centroid page: if `centroidHasPromotedForMonth` is false (Jan/Feb), falls back to old TrackCards + MonthNav sidebar. Will be removed once all months are reprocessed.

## Pending local state that's not in git

- `out/march_backfill/` — backfill log
- `out/beats_reextraction/RESUME_HERE.md` (older — 2026-04-14) — April pipeline resume notes, worth archiving or updating after March finishes
